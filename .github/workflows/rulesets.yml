name: Validate Rulesets

on:
  push:
    branches: [main]
    paths:
      - 'WPTechnix/**'
      - 'WPTechnix-PSR4/**'
      - 'WPTechnix-PSR/**'
      - 'WPTechnix-Strict/**'
      - 'tests/fixtures/**'
      - 'composer.json'
      - 'composer.lock'
      - '.github/workflows/rulesets.yml'
  pull_request:
    paths:
      - 'WPTechnix/**'
      - 'WPTechnix-PSR4/**'
      - 'WPTechnix-PSR/**'
      - 'WPTechnix-Strict/**'
      - 'tests/fixtures/**'
      - 'composer.json'
      - 'composer.lock'
      - '.github/workflows/rulesets.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.0', '8.3']
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer

      - name: Validate composer.json
        run: composer validate --strict

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Check ruleset XML is well-formed
        run: xmllint --noout WPTechnix/ruleset.xml WPTechnix-PSR4/ruleset.xml WPTechnix-PSR/ruleset.xml WPTechnix-Strict/ruleset.xml

      - name: List registered standards
        run: composer list-standards

      - name: Validate each standard resolves (standalone and combined)
        run: composer validate-rulesets

      - name: Lint style fixtures
        run: composer lint:fixtures
